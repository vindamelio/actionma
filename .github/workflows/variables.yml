name: Variables

on:
  push:
    branches: [main]

jobs:
  job1:
    name: job1
    runs-on: ubuntu-latest
 
    steps:
      - name: job1 step1
        run: |
          echo "Actor: ${{ github.actor }}"
          echo "Repository: ${{ github.repository }}"
          echo "Git Version: ${{ env.gitVer }}"
          echo "Git SHA: ${{ env.gitSha }}"
          echo "Git Tag: ${{ env.gitTag }}"

      